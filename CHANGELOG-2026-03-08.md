# 更新日志（2026-03-08）

## 新增功能
- 新增设备流转 `分配` 功能，位于 `出库` 与 `交接` 之间。
- 支持扫描后按设备逐台选择分配用户，再统一出库。
- 新增操作日志类型 `分配`。
- 操作日志新增 `设备持有人` 字段（用于区分操作人和实际持有人）。

## 时区能力
- 新增用户可长期保存的时区设置。
- 默认时区改为跟随用户系统时区。
- 扩充时区选项，覆盖各时区至少一个可选项。
- 前端在出入库、交接、日志查看、日志导出时按用户时区展示时间。
- 数据库存储保持 UTC，不修改数据库结构和历史数据。

## 设备管理与导出
- 设备管理页面取消 100 条显示上限。
- 导出规则按当前状态卡片生效：`总设备` 导出全部设备。
- 导出规则按当前状态卡片生效：`在库` 仅导出在库设备。
- 导出规则按当前状态卡片生效：`出库` 仅导出出库设备。
- 修复 Excel 导出异常问题。
- 修复设备筛选异常问题。

## 稳定性与交互修复
- 修复设备流转与设备管理日志未写入问题。
- 修复分配场景用户选择框显示不全问题。
- 分配场景用户选择交互对齐交接模块。

## 部署发布
- 已发布 Docker Hub 最新后端镜像（双架构）：`carloszhou/feishu-equipment-mgmt-backend:latest`。
- 已发布 Docker Hub 最新前端镜像（双架构）：`carloszhou/feishu-equipment-mgmt-frontend:latest`。
- 后端镜像包含架构：`linux/amd64`、`linux/arm64`。
- 前端镜像包含架构：`linux/amd64`、`linux/arm64`。

## 登录授权排查结论
- 现象：域名访问时飞书端提示 `requestAccess:fail please check errno`。
- 定位：失败发生在飞书前端 SDK `requestAccess` 阶段，后端未收到 `/api/auth/login`。
- 结论：属于飞书侧域名/协议校验问题，需确保访问域名与飞书应用配置一致，并优先使用 HTTPS 域名访问。
